# PhytoNote — Flutter app

Voir [`../README.md`](../README.md) pour la documentation complète.

Ce dossier contient le code Flutter de l'application. Build rapide :

```bash
flutter pub get
flutter run -d linux                            # dev desktop
flutter build apk --release --split-per-abi     # release Android
```
